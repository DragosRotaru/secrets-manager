#include <stdbool.h>
#include "../vault.h"

// https://www.beej.us/guide/bgnet/html/
// libevent, libuv, libev, poll() or select() for managing multiple sockets in a non-blocking fashion
// cjson, jsonc, protobuf or rolling my own data serialization

int serve(Vault vault, const bool readOnly) {
    return 0;
}